# Minimal channel for mock A/B harness (--mock-schedule-ab). Schedule rows come from
# MockAlternatingScheduleService; this file supplies program format for execution.
channel_id: test-1
number: 199
name: "Mock A/B harness"
channel_type: network
timezone: America/New_York

format:
  video: { width: 320, height: 240, frame_rate: "30000/1001" }
  audio: { sample_rate: 48000, channels: 2 }
